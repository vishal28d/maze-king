// contest details

enum ParticipateType {
  upcoming,
  live,
  completed,
}

enum WinningStatus {
  won,
  lose,
  review,
}

enum JoinStatus {
  start,
  exit,
  none,
}

enum ContestStatus {
  inReview,
  declared
}
